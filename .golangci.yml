version: "2"
run:
  timeout: 5m

linters:
  default: standard
  disable:
    - errcheck
  exclusions:
    generated: lax
    presets:
      - comments
    paths:
      - third_party$
      - builtin$
      - examples$
