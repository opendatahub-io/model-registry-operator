apiVersion: v1
kind: ServiceAccount
metadata:
  name: model-registry-grpc-{{.Name}}
  namespace: {{.Namespace}}
  labels:
    app: model-registry-grpc-{{.Name}}
    component: model-registry
    registry: {{.Name}}
